name: GitHub Actions Practice

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
   hello:
      runs-on: ubuntu-latest

      steps:
        - name: Say Hello
          run: echo "Hello! GitHub Actions is working"
